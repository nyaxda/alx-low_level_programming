#include <string.h>
#include "main.h"
/**
 * _strlen -  returns the length of a string.
 *
 * Description: function that returns the length of a string.
 *
 * @s: pointer to a char
 *
 * Return: void
 */
int _strlen(char *s)
{
	return (strlen(s));
}
