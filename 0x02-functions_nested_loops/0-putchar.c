#include <stdio.h>

/**
 * main - Entry Point.
 *
 * Description: prints '_putchar' with a newline.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	putchar(95);
	putchar(112);
	putchar(117);
	putchar(116);
	putchar(99);
	putchar(104);
	putchar(97);
	putchar(114);

	return (0);
}
